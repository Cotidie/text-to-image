# Local Models Directory

This directory stores locally downloaded Stable Diffusion models for offline use.

If you want to run a local model:

1. Download a local model and store it here (say `sd-turbo`)
2. Go to `.env` and edit `DEFAULT_MODEL` variable
3. Done.
